import React from 'react';//it is a core foundational library jo ki sare references lene ke kaam aatai hai
import ReactDOM from 'react-dom/client';// dom iska implementation haii web pe
//both the above libraries are used fo manipulating dom


import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
  
);

